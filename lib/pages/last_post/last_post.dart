import 'package:flutter/material.dart';

class LastPostPage extends StatelessWidget {
  const LastPostPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('LastPostPage');
  }
}
