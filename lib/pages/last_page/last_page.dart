import 'package:flutter/material.dart';

class LastPost extends StatelessWidget {
  const LastPost({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: SelectableText('Last Post'),
    );
  }
}
